You are an Implement subagent on branch feature/6-gpu-device-fix. A GPU integration bug wastes borrowed A800 time: the C1 phase runs the 7B model on CPU instead of GPU. Fix the device plumbing. Code only, offline, keep pytest green and MODEL-INDEPENDENT.

## The bug (verified on the A800)
- scripts/run_c1_facade.py (~line 595) hardcodes: `provider = HFActivationProvider(model, device="cpu", dtype="float32", cache_dir=...)`. So even on a GPU box, C1 forwards the 7B on CPU (fp32) — 166 cores pegged, GPU idle, ~31GB RAM.
- scripts/run_gpu_phase0.py builds its OWN provider with device=_pick_device() (cuda) for the C2b path (line ~142), but the C1 phase calls `c1.run(...)` which internally uses run_c1_facade's hardcoded-CPU provider. So C1 never touches the GPU.

## The fix (surgical)
1. In scripts/run_c1_facade.py: give `run(...)` (the main entry the runner calls) optional `device: str = "cpu"` and `dtype: str = "float32"` parameters (KEEP these defaults so local CPU behavior + all existing tests/callers are unchanged). Pass them through to the `HFActivationProvider(model, device=device, dtype=dtype, ...)` construction at ~line 595. If run_c1_facade also has a CLI argparse, add matching `--device`/`--dtype` flags defaulting to cpu/float32.
2. In scripts/run_gpu_phase0.py: when it calls `c1.run(...)`, pass `device=_pick_device(), dtype=_pick_dtype()` so on the A800 C1 runs on cuda/float16 (consistent with the C2b backend which already uses _pick_device/_pick_dtype). Verify the C2b path already passes cuda (it does at ~line 142-144) — leave that but make sure both phases use the SAME device/dtype.
3. Double-check the provider actually moves to GPU: provider._ensure_loaded does `model.to(self.device)` — confirm nothing else overrides device back to cpu. Confirm the activation-cache key includes device/dtype OR is fine to reuse across dtype (if the cache key doesn't include dtype, a cpu-fp32 cached activation could be wrongly reused for a gpu-fp16 run — if so, add device+dtype to the cache key so GPU runs don't read stale CPU-cached activations). Report what you found.

## Tests
- Keep `python -m pytest -q` GREEN and model-independent (no torch/model needed). Add a small test that run_c1_facade.run forwards device/dtype into the provider (you can monkeypatch/inspect the provider construction, or assert the argparse defaults + signature) and that run_gpu_phase0 passes _pick_device()/_pick_dtype() into c1.run. If the activation cache key needs device/dtype, add a test that two different (device,dtype) produce different cache keys.

CONSTRAINTS: work only in this worktree; DO NOT modify docs/. Commit to feature/6-gpu-device-fix with trailer:
Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
Do NOT merge, do NOT push if prompted for credentials.

Report: exact edits (file:line), whether the activation cache key needed the device/dtype fix (important — a stale CPU cache would silently corrupt the GPU run), pytest counts, and any judgment call.
