\begin{tikzpicture}[
  >=Latex,
  font=\footnotesize,
  node distance=8mm and 10mm,
  stagebox/.style={draw, rounded corners, align=center, minimum height=10mm, text width=28mm, inner sep=3pt},
  check/.style={draw, diamond, aspect=2.1, align=center, inner sep=1.5pt, text width=21mm},
  state/.style={draw, rounded corners, align=left, minimum height=15mm, text width=42mm, inner sep=4pt},
  unresolved/.style={state, dashed},
  warning/.style={state, double},
  supported/.style={state, line width=0.9pt},
  flow/.style={->, thick},
  branch/.style={font=\scriptsize, fill=white, inner sep=1pt}
]
  \node[stagebox] (candidate) {\textbf{Candidate axis}\\Named outcome + model, method, layer, task};
  \node[check, right=of candidate] (read) {\textbf{READ}\\supported?};
  \node[stagebox, right=of read] (compare) {\textbf{Bounded comparator}\\Select usable prompt and latent channels on DEV};
  \node[check, right=of compare] (transfer) {\textbf{TRANSFER}\\margin + coherence pass?};

  \node[unresolved, below=13mm of read] (unresolved) {\textbf{Unresolved}\\No semantic control label\\\scriptsize Evidence tier attached; record READ as unsupported or untested};
  \node[warning, below=13mm of compare] (withheld) {\textbf{Diagnostic or withheld control}\\Keep inspection only where READ supports it\\\scriptsize Attach calibration or resolution warning and exact evidence tier};
  \node[supported, below=13mm of transfer] (control) {\textbf{Evidence-supported control}\\Grant actionability only within the passing tier\\\scriptsize Preserve comparator, protocol, and coherence conditions};

  \draw[flow] (candidate) -- (read);
  \draw[flow] (read) -- node[branch, above]{yes} (compare);
  \draw[flow] (compare) -- (transfer);
  \draw[flow] (read) -- node[branch, left]{no / untested} (unresolved);
  \draw[flow] (transfer) -- node[branch, left]{no / underpowered} (withheld);
  \draw[flow] (transfer) -- node[branch, right]{yes} (control);
\end{tikzpicture}
