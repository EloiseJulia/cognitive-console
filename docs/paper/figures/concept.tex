\begin{tikzpicture}[
  >=Latex,
  font=\footnotesize,
  box/.style={draw, rounded corners, align=center, minimum height=12mm, text width=112mm, inner sep=4pt},
  flow/.style={->, thick},
  note/.style={draw, dashed, align=center, text width=142mm, inner sep=4pt}
]
  \node[box] (candidate) at (0,0) {\textbf{CANDIDATE AND EXACT EVIDENCE TIER}\\
    Model, method, direction and layer, task and outcome, protocol and version, comparator};
  \node[box] (result) at (0,-1.9) {\textbf{COMPUTATIONAL RESULT}\\
    READ support; comparator-bound TRANSFER result; coherence and calibration evidence};
  \node[box] (reason) at (0,-3.8) {\textbf{BLOCKING REASON}\\
    Record missing, mismatched, untested, failed, underpowered, incoherent, or measurement-limited evidence};
  \node[box] (action) at (0,-5.7) {\textbf{INTERFACE ACTION / ELIGIBILITY}\\
    READ may support a read-only diagnostic candidate within its tier. Active control is withheld unless the comparative gate passes within the exact same tier.};
  \node[note] (annotation) at (0,-7.45) {A comparative pass means only that active control passes the computational gate within the exact tier. It is not deployment permission or evidence of user benefit.};

  \draw[flow] (candidate) -- (result);
  \draw[flow] (result) -- (reason);
  \draw[flow] (reason) -- (action);
  \draw[flow] (action) -- (annotation);
\end{tikzpicture}
